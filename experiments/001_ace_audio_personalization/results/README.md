# Public experiment results

This index links to results that can be inspected without access to Henry's machine.
The working `artifacts/` directory is ignored by Git and is not a public result.

| Run | What is public | What is not yet public |
| --- | --- | --- |
| [September 12 synthetic smoke](smoke-2026-09-12.md) | Command, stage counts, verification boundary | Synthetic generated clips and temporary checkpoints |
| Real ACE GPU run | Not run yet | ACE-generated audio, GPU logs, and adapter |
| Original piano recordings | Not recorded or published yet | Audio and any companion MIDI |

When a real run is ready, add a dated result here with the exact model revision,
config, training-data provenance, listening samples, and evaluation notes. Publish
audio only when its rights and the performer's sharing choices are clear; do not
link to a local path or private cloud object as if it were a public artifact.
